# Development Dockerfile with hot reload
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json* ./
RUN npm ci

# Copy source (will be mounted in development)
COPY . .

# Expose port
EXPOSE 4000

# Start with hot reload
CMD ["npm", "run", "dev"]
